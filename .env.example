# QA MVP v1 and the local Corpus CLI require no API keys or environment variables.
# Subtitle processing runs locally; AI, Supabase and remote storage are not enabled.
# Do not place real credentials here. Add empty variable names and descriptions only
# when an implemented feature actually requires configuration.
